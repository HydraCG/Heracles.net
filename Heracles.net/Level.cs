namespace Heracles
{
    /// <summary>Provides possible {@link IHypermediaProcessor} support levels.</summary>
    public enum Level
    {
        /// <summary>Defines a not supported response.</summary>
        None = 0,

        /// <summary>Defines an exact support of the response.</summary>
        FullSupport = 100
    }
}